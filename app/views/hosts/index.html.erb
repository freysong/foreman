<% title_actions multiple_actions_select, button_group(new_link(_("New Host"))) %>

<p>
  Download SN:
  <%= link_to "CSV", hosts_path(format: "csv") %> |
  <%= link_to "Excel", hosts_path(format: "xls") %>
</p>

<%= render 'list', :hosts => @hosts, :header => @title || _("Hosts")  %>
<%= render 'listinfo', :hosts => @hosts, :header => @title || _("Hosts")  %>
